fastapi
uvicorn[standard]
pydantic
# R19: optimum is needed to load the ONNX model graph.
optimum[onnxruntime]
# R21: transformers is needed for the tokenizer.
transformers
# R26: FIX - Add Jinja2, which is required by the tokenizer's apply_chat_template function.
Jinja2
