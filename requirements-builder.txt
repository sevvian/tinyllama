# R59: The builder dependencies are now unpinned to allow pip to resolve
# the complex dependency graph for the very new Qwen3 model.
optimum[exporters]
# R59: We specify the minimum required version for transformers, as per the documentation.
transformers>=4.51.0
